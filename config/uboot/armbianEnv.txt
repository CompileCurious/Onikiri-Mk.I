verbosity=1
console=both
bootlogo=false
docker_optimizations=off

# Onikiri initramfs + immutable rootfs flow
rootdev=/dev/ram0
rootfstype=ramfs

# Use mainline BigTreeTech Pad7 DTB
fdtfile=sun50i-h616-bigtreetech-pad7.dtb
overlay_prefix=sun50i-h616

# Vendor-style boot parameters
extraargs=rdinit=/init rofs_device=/dev/mmcblk0p2 quiet splash loglevel=4 rootwait
